export declare global {
    namespace ReactNavigation {
        interface RootParamList {
            // Rotas que a aplicação possui
            movieList: undefined;
            movieDetails: {
                id: number,
            };
        }
    }
}
